class Ship {
    /**
     * This function is called by JavaScript whenever someone creates a new instance
     * of a Ship. It should initialize any variables used by other functions below
     */
    constructor() {
    }

    /**
     * Anything that should be calculated each frame should be put here. This would
     * include things like movement, keypresses, or if we have been hit.
     */
    update() {
    }

    /**
     * Draws our player's ship (which is a triangle). This function should
     * be careful if it changes any of the global drawing settings (like color,
     * fill, etc.) to change them back when it's done.
     */
    draw() {
    }

    /**
     * Fire one bullet. This adds a bullet to the array currently being tracked.
     * It starts at the nose of the ship and will move forward with each frame.
     */
    fire() {
    }
}

class Bullet {
    /**
     * This function is called by JavaScript whenever someone creates a new instance
     * of a Bullet. It should initialize any variables used by other functions below
     */
    constructor() {
    }

    /**
     * Anything that should be calculated each frame should be put here. This would
     * include things like movement or if we have hit an enemy.
     */
    update() {
    }

    /**
     * Draws one of the fired bullets (which is a circle). This function should
     * be careful if it changes any of the global drawing settings (like color,
     * fill, etc.) to change them back when it's done.
     */
    draw() {
    }
}

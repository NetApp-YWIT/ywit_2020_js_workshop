class Enemy {
    /**
     * This function is called by JavaScript whenever someone creates a new instance
     * of an Enemy. It should initialize any variables used by other functions below
     */
    constructor() {
    }

    /**
     * Anything that should be calculated each frame should be put here. This would
     * include things like movement or if we have been hit by a bullet or hit the
     * player's ship.
     */
    update() {
    }

    /**
     * Draws the enemy (which is a square). This function should be careful if
     * it changes any of the global drawing settings (like color, fill, etc.) to
     * change them back when it's done.
     */
    draw() {
    }

    /**
     * This function will determine if two enties have collided. This will either
     * be an enemy and a bullet or an enemy and the player's ship. It should
     * return true or false.
     */
    collide() {
    }
}
